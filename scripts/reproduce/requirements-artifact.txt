# Artifact reproduction dependencies
# Install: pip install -r scripts/reproduce/requirements-artifact.txt

# Core (already in pyproject.toml)
# pydantic>=2.0
# pyyaml>=6.0
# networkx>=3.0

# Phase 2 interoperability (new)
rdflib>=7.0
pyshacl>=0.25
cryptography>=42.0
