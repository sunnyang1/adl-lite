repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0
    hooks:
      - id: ruff
        args: [--fix]
        exclude: ^(archive/|data/aml/)  # historical/legacy (AGENTS.md)
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.15.0
    hooks:
      - id: mypy
        args: [--ignore-missing-imports]
        exclude: ^(archive/|data/aml/)  # historical/legacy; ruff excludes both (AGENTS.md)
        additional_dependencies:
          - pydantic>=2.0
          - types-PyYAML
          - networkx>=3.0
