\section{Standards Mapping and Loss Analysis}\label{app:loss-analysis}

This appendix provides the complete bidirectional mapping tables and quantitative loss analysis for ADL Lite's interoperability with PROV-O, SHACL, and related Semantic Web standards. The analysis is organised by standard, with preservation metrics computed from the 201 AML EventChains (9,300 events) used in E12.

\subsection{PROV-O Bidirectional Mapping}

Table~\ref{app:tab:prov-full} expands the PROV-O mapping from Table~\ref{tab:prov-mapping-full} with all datatype properties, object properties, and their OWL~2~DL compatibility.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Complete ADL Lite $\leftrightarrow$ PROV-O property mapping with OWL~2~DL compatibility.}
\label{app:tab:prov-full}
\begin{tabular}{@{}p{2.6cm}p{3.0cm}p{2.4cm}p{2.6cm}@{}}
\toprule
\textbf{ADL Property} & \textbf{PROV-O Property} & \textbf{OWL~2~DL?} & \textbf{Round-trip} \\
\midrule
\texttt{event\_id} & \texttt{adl:eventId} (literal) & Yes (datatype) & Full \\
\texttt{event\_type} & \texttt{rdf:type} (subclass) & Yes (class) & Full \\
\texttt{actor} & \texttt{prov:wasAssociatedWith} & Yes (object) & Full \\
\texttt{timestamp} & \texttt{prov:startedAtTime} & Yes (datatype) & Full \\
\texttt{payload} & \texttt{adl:payload} (JSON) & Yes (datatype) & Full \\
\texttt{hash} & \texttt{adl:eventHash} & Yes (datatype) & Full \\
\texttt{previous\_hash} & \texttt{adl:previousEventHash} & Yes (datatype) & Full \\
\texttt{previous\_event\_id} & \texttt{prov:wasInformedBy} & Yes (object) & Full \\
\texttt{concept\_id} & \texttt{adl:conceptId} & Yes (datatype) & Full \\
\texttt{status} (derived) & \texttt{adl:status} & Yes (datatype) & Derived$^a$ \\
\texttt{confidence} (derived) & \texttt{adl:confidence} & Yes (datatype) & Derived$^a$ \\
\texttt{validators} (set) & \texttt{prov:wasAttributedTo} (multiple) & Yes (object) & Full \\
L3 \texttt{predicate} & \texttt{prov:wasDerivedFrom} + qualifier & Yes (object) & Partial$^b$ \\
L3 \texttt{confidence} & RDF-star \texttt{adl:confidence} & No (RDF-star) & Partial$^b$ \\
L3 \texttt{evidence} & RDF-star \texttt{adl:evidenceRef} & No (RDF-star) & Partial$^b$ \\
\bottomrule
\multicolumn{4}{@{}l@{}}{$^a$Derived on import by recomputing $\delta(C)$ / $\gamma(C)$; exported values are cached snapshots.} \\
\multicolumn{4}{@{}l@{}}{$^b$Plain RDF loses reified metadata; RDF-star preserves it as quoted triples.}
\end{tabular}
\end{table}

\subsection{SHACL Shape Coverage for L3 and L4 Constraints}

The SHACL shape graph (\texttt{formal/owl/adl\_shacl\_shapes.ttl}) validates six structural constraints. Table~\ref{app:tab:shacl-coverage} reports shape coverage per constraint type.

\begin{table}[htbp]
\centering
\footnotesize
\caption{SHACL shape coverage for ADL Lite document constraints.}
\label{app:tab:shacl-coverage}
\begin{tabular}{@{}p{4.2cm}ccc@{}}
\toprule
\textbf{Constraint} & \textbf{SHACL Core} & \textbf{SHACL-SPARQL} & \textbf{Status} \\
\midrule
Identifier format (UUID) & \checkmark & --- & Validated \\
Predicate membership (closed set) & \checkmark & --- & Validated \\
Confidence range $[0,1]$ & \checkmark & --- & Validated \\
Mapping type enumeration & \checkmark & --- & Validated \\
Symmetric relation consistency & \checkmark & --- & Validated \\
Directionality enforcement & --- & \checkmark & Validated \\
Cross-document concept existence & --- & \checkmark & Validated \\
Conditional self-reference & --- & \checkmark & Validated \\
\bottomrule
\end{tabular}
\end{table}

Of the eight L3/L4 constraints, five are expressible in SHACL Core and three require SHACL-SPARQL. The precondition language (eight comparators over snapshot fields) is not expressible in SHACL because it requires evaluating derived values ($\delta$, $\gamma$) at validation time, which exceeds the expressivity of both SHACL Core and SHACL-SPARQL (which operates over the RDF graph, not over an external derivation function).

\subsection{Quantitative Preservation Metrics}

Table~\ref{app:tab:quant-preservation} reports the preservation rate for each construct category, computed as the fraction of constructs that survive round-trip export/import without information loss.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Quantitative preservation metrics (201 chains, 9,300 events).}
\label{app:tab:quant-preservation}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Category} & \textbf{Entities} & \textbf{Preserved} & \textbf{Rate} & \textbf{Loss Mode} \\
\midrule
Event identity & 9,300 & 9,300 & 100.0\% & None \\
Cryptographic linkage & 9,300 & 9,300 & 100.0\% & None \\
Actor & 9,300 & 9,300 & 100.0\% & None \\
Timestamp & 9,300 & 9,300 & 100.0\% & None \\
Payload (JSON) & 9,300 & 9,300 & 100.0\% & None \\
L3 relation predicate & 847 & 847 & 100.0\% & None \\
L3 relation confidence & 847 & 296 & 35.0\% & Plain RDF reification \\
L3 relation evidence & 847 & 296 & 35.0\% & Plain RDF reification \\
Status $\delta(C)$ & 201 & 201 & 100.0\%$^a$ & Derived on import \\
Confidence $\gamma(C)$ & 201 & 201 & 100.0\%$^a$ & Derived on import \\
Precondition rules & 1,024 & 0 & 0.0\% & Exceeds expressivity \\
\bottomrule
\multicolumn{5}{@{}l@{}}{$^a$Not stored in RDF; recomputed from events on import, yielding deterministic consistency.}
\end{tabular}
\end{table}

The L3 relation confidence/evidence loss (65\%) occurs when exporting to plain RDF (without RDF-star). When RDF-star is enabled, the preservation rate rises to 100\% for all relation constructs, because confidence and evidence are encoded as quoted-triple annotations.

\subsection{Asymmetric Bridge Discussion}

The ADL Lite $\leftrightarrow$ PROV-O bridge is \emph{asymmetric} in two respects.

\textbf{Export asymmetry (ADL $\to$ PROV-O).} The export direction is \emph{lossy but useful}: all structural information (events, actors, hashes, timestamps) is preserved, but the application semantics ($\delta$, $\gamma$, preconditions) are not encoded in the RDF. This is intentional: PROV-O is a general-purpose provenance standard, and ADL Lite's derivation functions are application-specific. The exported RDF is intended for consumption by standard RDF tools (e.g., SPARQL endpoints, triple stores) that do not need to understand ADL Lite's lifecycle semantics.

\textbf{Import asymmetry (PROV-O $\to$ ADL).} The import direction is \emph{conservative}: the importer reconstructs the EventChain and recomputes all derived values from the event history, rather than trusting the cached literals in the RDF. This ensures that even if the exported RDF was tampered with (e.g., the \texttt{adl:status} literal was changed), the imported chain will have the \emph{correct} status as determined by $\delta(C)$. The only way to corrupt the imported chain is to modify the event hashes, which breaks \texttt{verify\_integrity()}.

\textbf{Scope of the bridge.} The bridge is not a general-purpose ontology alignment; it is a \emph{specialised interoperability profile} for capability-lifecycle audit. It assumes that the PROV-O activities being imported are lifecycle events (register, validate, deprecate, fork, archive) rather than arbitrary provenance traces. General-purpose PROV-O traces (e.g., from a scientific workflow) can be imported, but their activities will be typed as generic \texttt{adl:Activity} events unless they match one of the five lifecycle subclasses.

\subsection{PROV-O Profile: Complete Turtle Serialisation}

Listing~\ref{app:lst:prov-full-profile} provides the complete normative Turtle serialisation of the ADL Lite PROV-O profile, including namespace declarations and class hierarchy.

\begin{lstlisting}[language=Turtle, caption={Complete ADL Lite PROV-O profile (Turtle serialisation).}, label={app:lst:prov-full-profile}]
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix prov: <http://www.w3.org/ns/prov#> .
@prefix adl: <https://adl-lite.org/ns/> .

adl:RegisterActivity  a owl:Class ;
    rdfs:subClassOf prov:Activity ;
    rdfs:label "ADL Register Activity"@en ;
    rdfs:comment "Genesis event for a capability concept."@en .

adl:ValidateActivity  a owl:Class ;
    rdfs:subClassOf prov:Activity ;
    rdfs:label "ADL Validate Activity"@en ;
    rdfs:comment "Validation event with confidence payload."@en .

adl:DeprecateActivity a owl:Class ;
    rdfs:subClassOf prov:Activity ;
    rdfs:label "ADL Deprecate Activity"@en ;
    rdfs:comment "Lifecycle termination event."@en .

adl:ForkActivity      a owl:Class ;
    rdfs:subClassOf prov:Activity ;
    rdfs:label "ADL Fork Activity"@en ;
    rdfs:comment "Child-chain creation event."@en .

adl:ArchiveActivity   a owl:Class ;
    rdfs:subClassOf prov:Activity ;
    rdfs:label "ADL Archive Activity"@en ;
    rdfs:comment "Cold-storage event."@en .

adl:eventHash         a owl:DatatypeProperty ;
    rdfs:domain  prov:Activity ;
    rdfs:range   xsd:string ;
    rdfs:label   "SHA-256 hash of canonical event serialisation"@en .

adl:previousEventHash a owl:DatatypeProperty ;
    rdfs:domain  prov:Activity ;
    rdfs:range   xsd:string ;
    rdfs:label   "SHA-256 hash of predecessor event"@en .

adl:conceptId         a owl:DatatypeProperty ;
    rdfs:domain  prov:Entity ;
    rdfs:range   xsd:string ;
    rdfs:label   "Human-readable ADL concept identifier"@en .

adl:payload          a owl:DatatypeProperty ;
    rdfs:domain  prov:Activity ;
    rdfs:range   xsd:string ;
    rdfs:label   "JSON-serialised event payload"@en .

adl:eventIndex       a owl:DatatypeProperty ;
    rdfs:domain  prov:Activity ;
    rdfs:range   xsd:integer ;
    rdfs:label   "Zero-based index in EventChain"@en .
\end{lstlisting}
